#!/bin/bash

#Write a script that takes a string as an argument and counts the number of vowels in the string
string=$1
count=0
vowCount=$(echo $string | grep -o "[aeiouAEIOU]" | wc -l)
echo "the no of vowels in string is $vowCount"
